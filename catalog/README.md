# Catalogue

Catalogue JSON des clips (URL, durée, tags, source, licence, crédit) et presets des styles et univers. Rempli à partir du jalon 4 par le pipeline de `/tools`. Hébergé en statique sur GitHub Pages (CORS `*` par défaut, compatible textures WebGL).
